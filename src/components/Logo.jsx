import React from 'react';
import logo from '../img/logoHome.png';

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="logo"/>
        </div>
    );
};

export default Logo;